plugin {
    split-monitor-workspaces {
        count = 4
        keep_focused = 0
        enable_notifications = 0
        enable_persistent_workspaces = 1
    }
    hyprbars {
        # example config
        bar_height = 18
        bar_color = rgba(282a36cf)

        bar_text_size = 10
        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgba(db6030af), 14, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgba(eeee11af), 14, , hyprctl dispatch fullscreen 1
        bar_buttons_alignment = left
        bar_part_of_window = true

        # cmd to run on double click of the bar
        on_double_click = hyprctl dispatch fullscreen 1
        icon_on_hover = true
    }
    hyprscrolling {
        column_width = 0.7
        fullscreen_on_one_column = false
    }
    hy3 {
        # disable gaps when only one window is onscreen
        # 0 - always show gaps
        # 1 - hide gaps with a single window onscreen
        # 2 - 1 but also show the window border
        no_gaps_when_only = 0 # default: 0

        # policy controlling what happens when a node is removed from a group,
        # leaving only a group
        # 0 = remove the nested group
        # 1 = keep the nested group
        # 2 = keep the nested group only if its parent is a tab group
        node_collapse_policy = 0 # default: 2

        # offset from group split direction when only one window is in a group
        group_inset = 10 # default: 10

        # if a tab group will automatically be created for the first window spawned in a workspace
        tab_first_window = false

        # tab group settings
        tabs {
            # height of the tab bar
            height = 14 # default: 15

            # padding between the tab bar and its focused node
            padding = 1 # default: 5

            # the tab bar should animate in/out from the top instead of below the window
            from_top = true # default: false

            radius=1

            # rounding of tab bar corners
            rounding = 1 # default: 3

            # render the window title on the bar
            render_text = true # default: true

            # center the window title
            text_center = true # default: false

            # font to render the window title with
            text_font = SFMono Nerd Font # default: Sans

            # height of the window title
            text_height = 11 # default: 8

            # left padding of the window title
            text_padding = 3 # default: 3

            # active tab (selected and focused)
            col.active = rgba(44475aee)         # 暗灰蓝背景
            col.active.border = rgba(bd93f9af)  # 紫色边框
            col.active.text = rgba(f8f8f2ff)    # 明亮文字

            # focused tab (focused node in unfocused container)
            col.focused = rgba(6272a4cc)        # 深蓝背景
            col.focused.border = rgba(8be9fdaf) # 青色边框
            col.focused.text = rgba(f8f8f2ff)   # 明亮文字

            # inactive tab
            col.inactive = rgba(282a3690)       # 主背景色（较深）
            col.inactive.border = rgba(44475aaf)# 较暗边框
            col.inactive.text = rgba(bfbfbfff)  # 中亮文字

            # urgent tab
            col.urgent = rgba(ff555570)         # 红色警示背景
            col.urgent.border = rgba(ff5555af)  # 红色边框
            col.urgent.text = rgba(f8f8f2ff)    # 明亮文字

            # locked tab
            col.locked = rgba(f1fa8c60)         # 黄色背景
            col.locked.border = rgba(f1fa8caf)  # 黄色边框
            col.locked.text = rgba(282a36ff)    # 深色文字（和黄色对比强）

            # blur settings
            blur = true

            # overall opacity multiplier
            opacity = 0.8
        }

        # autotiling settings
        autotile {
            # enable autotile
            enable =true # default: false

            # make autotile-created groups ephemeral
            ephemeral_groups = false # default: true

            # if a window would be squished smaller than this width, a vertical split will be created
            # -1 = never automatically split vertically
            # 0 = always automatically split vertically
            # <number> = pixel height to split at
            trigger_width = 0 # default: 0

            # if a window would be squished smaller than this height, a horizontal split will be created
            # -1 = never automatically split horizontally
            # 0 = always automatically split horizontally
            # <number> = pixel height to split at
            trigger_height = 0 # default: 0

            # a space or comma separated list of workspace ids where autotile should be enabled
            # it's possible to create an exception rule by prefixing the definition with "not:"
            # workspaces = 1,2 # autotiling will only be enabled on workspaces 1 and 2
            # workspaces = not:1,2 # autotiling will be enabled on all workspaces except 1 and 2
            workspaces = all # default: all
        }
    }
    overview {
        disableBlur = true
        dragAlpha = 0.8
        panelHeight = 200
        gapsOut = 10
    }
    hych {
        enable_alt_release_exit = 1 # alt swith mode,see readme for detail
        alt_replace_key = code:64 # alt swith mode,see readme for detail
    }
}
